webpackJsonp([0x79c00a746f55],{400:function(e,t){e.exports={data:{markdownRemark:{html:'<p>Over the past few days, I have discovered the large potential of falsey values (<code>false</code>, <code>0</code>, <code>""</code>, <code>null</code>, <code>undefined</code>, <code>NaN</code>) to provide direction and simplify code in javascript.  If used correctly, one can replace multi-line logical expressions with logical operator expressions.</p>\n<h3>Counting example</h3>\n<p>To exhibit this point, let’s take a look at the example of decrementing a number variable named <code>count</code>.  In this example, when <code>count</code> reaches zero, we want the program to stop decrementing <code>count</code>, as exhibited below.</p>\n<pre><code class="language-javascript">if ( count > 0 ){\n  count--;\n}\n</code></pre>\n<p>However, a subtle simplification can be made in line 1 using our understanding of fasey values; when the value of <code>count</code> changes to zero, the evaluation of <code>count</code> returns false because because zero is a falsey value.  Therefore, we could rewrite the example as follows:</p>\n<pre><code class="language-javascript">if ( count ){\n  count--;\n}\n</code></pre>\n<h3>Reasoning</h3>\n<p>The reason these two examples reach the same outcome is because if <code>count</code> is a number greater than zero, it is inherently non-falsey and will therefore evaluate to true.  Alternatively, if <code>count</code> is set to zero, the statement will evaluate to false and <code>count</code> will stop decrementing.  Although our line count does not change between the two examples, this fundamental understanding that falsey values evaluate to false is essential to continue.  Next, we need to understand how logical operators function within javascript.  For this example, let’s analyze the and (<code>&#x26;&#x26;</code>) evaulation.  The following four conditions are possible using the <code>&#x26;&#x26;</code> evaluation:</p>\n<ol>\n<li><code>true &#x26;&#x26; true;</code>      returns the second truthy case</li>\n<li><code>true &#x26;&#x26; false;</code>     returns the falsey case</li>\n<li><code>false &#x26;&#x26; true;</code>     returns the falsey case</li>\n<li><code>false &#x26;&#x26; false;</code>    returns the first falsey case</li>\n</ol>\n<p>Now, when we combine falsey values and logical operators, we see how the above example can be rewritten and simplified to the following single-line logical expression:</p>\n<pre><code class="language-javascript">count &#x26;&#x26; count--;\n</code></pre>\n<p>If the value of <code>count</code> is greater than zero, <code>count</code> evaluates to true; however, the expression of decrementing the <code>count</code> variable is also true, therefore javascript returns the second truthy condition, i.e. decrementing the <code>count</code> variable.  When the <code>count</code> variable reaches zero, we now trigger a false condition and this line will essentially be skipped by javascript.  The table below depicts an example of this scenario where <code>count</code> is initially set to 5.</p>\n<table>\n<thead>\n<tr>\n<th><code>count</code>\n value pre-invocation</th>\n<th>evaluation of \n<code>count &#x26;&#x26; count–</code></th>\n<th>returned value</th>\n<th><code>count</code>\n value post-invocation</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>5</code></td>\n<td><code>true &#x26;&#x26; true</code></td>\n<td><code>true</code>\n (count–)</td>\n<td><code>4</code></td>\n</tr>\n<tr>\n<td><code>4</code></td>\n<td><code>true &#x26;&#x26; true</code></td>\n<td><code>true</code>\n (count–)</td>\n<td><code>3</code></td>\n</tr>\n<tr>\n<td><code>3</code></td>\n<td><code>true &#x26;&#x26; true</code></td>\n<td><code>true</code>\n (count–)</td>\n<td><code>2</code></td>\n</tr>\n<tr>\n<td><code>2</code></td>\n<td><code>true &#x26;&#x26; true</code></td>\n<td><code>true</code>\n (count–)</td>\n<td><code>1</code></td>\n</tr>\n<tr>\n<td><code>1</code></td>\n<td><code>true &#x26;&#x26; true</code></td>\n<td><code>true</code>\n (count–)</td>\n<td><code>0</code></td>\n</tr>\n<tr>\n<td><code>0</code></td>\n<td><code>false &#x26;&#x26; true</code></td>\n<td><code>false</code>\n (count)</td>\n<td><code>0</code></td>\n</tr>\n<tr>\n<td><code>0</code></td>\n<td><code>false &#x26;&#x26; true</code></td>\n<td><code>false</code>\n (count)</td>\n<td><code>0</code></td>\n</tr>\n</tbody>\n</table>\n<h3>Summary</h3>\n<p>Additionally, below are two fully functioning recursive examples, one using the traditional approach (<code>example1</code>) and one using the simplified method described (<code>example2</code>).</p>\n<pre><code class="language-javascript">var example1 = function(count){\n  if ( count === 0 ){\n    return (“Finished decrementing!”);\n  } else {\n    count--;\n  }\n  console.log(count);\n  example1(count);\n};\n\nexample1(5);\n</code></pre>\n<pre><code class="language-javascript">var example2 = function(count){\n  if ( count &#x26;&#x26; count-- ) {\n    console.log(count);\n    example2(count);\n  }\n  return (“Finished decrementing!”);\n};\n\nexample2(5);\n</code></pre>',frontmatter:{date:"1406703600000",path:"/blog/supercharge-your-falsey-conditional-statements-in-javascript",title:"Supercharge your falsey && conditional statements in JavaScript"}}},pathContext:{}}}});
//# sourceMappingURL=path---blog-supercharge-your-falsey-conditional-statements-in-javascript-39da70760aea44b9ed25.js.map